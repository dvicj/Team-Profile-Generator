//parent class

//methods: name, id, email, getName(), getId(), getEmail(), getRole() - returns Employee

function Employee (name, title) {
    this.name = name;
    this.title = title; 
    this.id = id; 
    this.email = email; 
    this.number = number; 
}